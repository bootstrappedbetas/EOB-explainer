# Auth0 credentials - get these from https://manage.auth0.com
# 1. Create an Auth0 account
# 2. Create an Application (Single Page Application)
# 3. Add http://localhost:5173 to Allowed Callback URLs, Logout URLs, and Web Origins
# 4. Enable Database (email/password) and Google connections in Auth0 Dashboard

VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your_client_id_here

# Auth0 API (for backend JWT validation) - create an API in Auth0 Dashboard
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_AUDIENCE=https://your-api.com

# Optional: for organization SSO
# VITE_AUTH0_ORGANIZATION=org_xxxxxxxx

# Database - PostgreSQL connection string
# Local: postgresql://user:password@localhost:5432/eob_explainer
# Supabase/Neon: use their connection string
DATABASE_URL=postgresql://localhost:5432/eob_explainer

# For production DBs (Supabase, Neon) that use SSL
# DATABASE_SSL=true

# Server
PORT=3000
CORS_ORIGIN=http://localhost:5173

# AI summary (OpenAI)
OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o-mini
